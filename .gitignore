# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Test / cache
.pytest_cache/
.coverage
htmlcov/

# Type-checker cache
.mypy_cache/
.ruff_cache/

# Local event stores / runtime data
*.db
*.db-journal
*.db-wal
*.db-shm
data/

# Virtualenvs
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Windows device-name artifacts (redirecting to NUL can create a literal file)
nul

# Stray cron/workflow temp files with mangled Windows paths
# (Windows replaces ':' with U+F03A in filenames, so match broadly)
C*Users*

# Local build output (do not ship build artifacts in the repo)
dist-build/
*.whl
*.tar.gz
